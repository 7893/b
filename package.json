{
  "name": "wordpress-hugo-migration",
  "version": "1.0.0",
  "description": "Migration of WordPress content to Hugo with Cloudflare deployment",
  "main": "index.js",
  "scripts": {
    "deploy": "wrangler pages deploy public --project-name=b",
    "build": "hugo --minify"
  },
  "devDependencies": {
    "@cloudflare/wrangler": "^3.0.0"
  },
  "dependencies": {
    "wrangler": "^3.0.0"
  }
}